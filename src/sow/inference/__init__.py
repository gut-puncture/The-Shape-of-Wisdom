"""
Stage 13/14: full inference + analysis.

We keep this separate from PCA sample inference (Stage 11) because Stage 13 has
strict gating (batch-consistency + resume simulation) and a larger output schema.
"""

